rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /customers/{email} {
      allow read, write: if request.auth.email == email;
    }
    
    match /{document=**} {
      allow read, write: if false;
    }
  }
}